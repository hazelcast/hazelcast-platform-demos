<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.hazelcast.platform.demos.industry.iiot</groupId>
		<artifactId>iiot-test-root</artifactId>
		<version>5.0</version>
	</parent>

	<artifactId>iiot-test-abstract-client</artifactId>

	<name>Hazelcast Platform - IIOT - Test - Abstract Client</name>
	<description>Shared code for client testing against Hazelcast Cloud and local cluster</description>

	<properties>
		<main.basedir>${project.parent.parent.parent.parent.basedir}</main.basedir>
	</properties>

	<dependencies>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>iiot-custom-classes</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- Dependencies with scope "provided" in the above -->
		<dependency>
			<groupId>com.hazelcast</groupId>
			<artifactId>hazelcast-sql</artifactId>
		</dependency>
	</dependencies>

	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
		</resources>
	</build>

</project>
