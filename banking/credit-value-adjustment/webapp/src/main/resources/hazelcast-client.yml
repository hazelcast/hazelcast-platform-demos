hazelcast-client:
  # Replaced by Maven
  instance-name: '@project.artifactId@'
  network:
    # Default, but make it obvious webapp can multiplex
    smart-routing: true
  metrics:
    enabled: true
