hazelcast:
  # Replaced by Maven
  cluster-name: '@my.cluster.name@'
  # Replaced by Maven
  license-key: '@my.jet.license.key@'
  #
  # Assuming Kubernetes, but override from Java code if we detect otherwise
  network:
    join:
      kubernetes:
        enabled: true
        service-dns: '@my.cluster.name@-service.default.svc.cluster.local'
  