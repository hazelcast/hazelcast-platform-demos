<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

        <parent>
                <groupId>com.hazelcast.platform.demos.telco.churn</groupId>
                <artifactId>churn-root</artifactId>
                <version>4.0</version>
        </parent>

	<artifactId>churn-nodejs-client</artifactId>

	<name>Hazelcast Platform - Churn - Hazelcast Node.js Client</name>
	<description>Telco Churn - Hazelcast NodeJS Client</description>

	<properties>
		<docker.image.name>nodejs-client</docker.image.name>
		<docker.jar.file>${project.artifactId}-${project.version}.jar</docker.jar.file>
		<main.basedir>${project.parent.parent.parent.basedir}</main.basedir>
		<my.cluster.name>${my.cluster1.name}</my.cluster.name>
	</properties>

	<dependencies>
	</dependencies>

	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
		</resources>

		<plugins>
			<plugin>
				<groupId>com.spotify</groupId>
				<artifactId>dockerfile-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
</project>
